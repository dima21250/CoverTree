# Use GPU for FAISS acceleration (set by CMake option USE_GPU)
USE_GPU = @PYTHON_USE_GPU@
# Use OpenAI embeddings (set by CMake option USE_OPENAI_EMBEDDINGS)
USE_OPENAI_EMBEDDINGS = @PYTHON_USE_OPENAI_EMBEDDINGS@
